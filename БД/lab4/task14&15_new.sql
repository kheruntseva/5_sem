-- Проверка номера последнего архива
SELECT MAX(sequence#) AS last_archive_number
FROM v$archived_log;

-- Если результат NULL или 0, архивов нет (архивирование отключено)
-- Если число >0, это номер последнего архива